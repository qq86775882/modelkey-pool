version: "3.8"

services:
  modelkey-pool:
    build: .
    container_name: modelkey-pool
    ports:
      - "${PROXY_PORT:-8000}:8000"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
