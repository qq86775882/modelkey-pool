# ModelScope API Keys，逗号分隔（初始 Key，后续可通过管理面板 CRUD）
MODELSCOPE_KEYS=ms-your_key_1,ms-your_key_2

# 每把 Key 每天的调用上限
DAILY_LIMIT=200

# 默认模型
DEFAULT_MODEL=deepseek-ai/DeepSeek-V4-Pro

# Netlify 环境会自动注入以下变量，本地开发无需设置：
# NETLIFY_BLOBS_CONTEXT — Blobs 连接信息
